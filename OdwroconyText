#include <iostream>
#include <conio.h>
#include <string>

using namespace std;

int main()
{
    string napis;

    cout << "Podaj swoj napis: ";
    cin >> napis;

    for (int i = 1; i <= napis.size(); i++)

    {
        cout << napis[napis.size() - i];
    }

    int getch();
}
